%tr
  %td= order.id
  %td= order.created_at
  %td= number_to_money order.sum
  -if in_progress
    %td= link_to t('orders.show'), confirm_checkout_path(order)
  -else
    %td= link_to t('orders.show'), order
