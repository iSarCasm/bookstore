%section#index
  .container
    .row
      .col-sm-8
        %h2 Books
        = render @books
        .row.page-pagination
          = paginate @books
      .col-sm-4
        %h2 Categories
        = render @categories
